name: Deploy cleaner to k3s

on:
  push:
    branches:
      - main
    paths:
      - 'sensor-api/**'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build and push Docker image
        env:
          REGISTRY_HOST: ${{ secrets.REGISTRY_HOST }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t ${REGISTRY_HOST}:5000/cleaner:${IMAGE_TAG} sensor-api/
          docker tag ${REGISTRY_HOST}:5000/cleaner:${IMAGE_TAG} ${REGISTRY_HOST}:5000/cleaner:latest
          docker push ${REGISTRY_HOST}:5000/cleaner:${IMAGE_TAG}
          docker push ${REGISTRY_HOST}:5000/cleaner:latest

      - name: Deploy to k3s
        env:
          REGISTRY_HOST: ${{ secrets.REGISTRY_HOST }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          sudo kubectl create namespace cleaner --dry-run=client -o yaml | sudo kubectl apply -f -
          sed -e "s|__REGISTRY_HOST__|${REGISTRY_HOST}|g" \
              -e "s|__IMAGE_TAG__|${IMAGE_TAG}|g" \
              sensor-api/manifests/cronjob.yaml | sudo kubectl apply -f - -n cleaner

      - name: Notify Slack on success
        if: success()
        run: |
          curl -s -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d '{"text":"✅ cleaner deploy succeeded: `${{ github.ref_name }}` @ `${{ github.sha }}`"}'

      - name: Notify Slack on failure
        if: failure()
        run: |
          curl -s -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d '{"text":"❌ cleaner deploy failed: `${{ github.ref_name }}` @ `${{ github.sha }}`"}'
